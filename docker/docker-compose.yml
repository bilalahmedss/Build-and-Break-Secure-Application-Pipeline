services:
  webapp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: build-and-break-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=0
      - FLASK_RUN_HOST=0.0.0.0
    restart: unless-stopped
